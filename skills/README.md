# Registered Skills

- `executar-mapa-os` — operational Mapa-OS projection/orchestration skill.
- `copiloto-executar` — daily EXECUTAR operational copiloto skill.

Each registered skill includes its canonical `SKILL.md` entrypoint and a `SOURCE_MANIFEST.json` with the SHA-256 provenance of the user-supplied archive. Registration does not imply production approval or deployment.
